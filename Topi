<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Para Ana</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <div class="container">
    <h1>Desde que nossos corações se encontraram...</h1>
    <div id="timer"></div>
    <p class="mensagem">Cada segundo ao seu lado é um presente. Te amo, Ana!</p>

    <div class="galeria">
      <!-- Adicione suas imagens aqui -->
      <img src="img/1.jpg" alt="Foto 1" />
      <img src="img/2.jpg" alt="Foto 2" />
      <img src="img/3.jpg" alt="Foto 3" />
      <!-- Continue adicionando mais conforme precisar -->
    </div>
  </div>

  <script src="script.js"></script>
</body>
</html>
body {
  margin: 0;
  font-family: 'Segoe UI', cursive, sans-serif;
  background: linear-gradient(to bottom, #ffd6e8, #fff0f5);
  color: #c2185b;
  text-align: center;
}

.container {
  padding: 2rem;
}

h1 {
  font-size: 2.5rem;
  margin-bottom: 1rem;
}

.mensagem {
  font-size: 1.2rem;
  margin-top: 1rem;
  font-style: italic;
}

#timer {
  font-size: 2rem;
  margin-top: 1rem;
  font-weight: bold;
  color: #880e4f;
}

.galeria {
  margin-top: 2rem;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1rem;
}

.galeria img {
  max-width: 200px;
  border-radius: 15px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.2);
  transition: transform 0.3s ease;
}

.galeria img:hover {
  transform: scale(1.05);
}function atualizarTempo() {
  const inicio = new Date('2023-06-12T09:32:00'); // 12 de junho às 9:32
  const agora = new Date();
  const diff = agora - inicio;

  const dias = Math.floor(diff / (1000 * 60 * 60 * 24));
  const horas = Math.floor((diff / (1000 * 60 * 60)) % 24);
  const minutos = Math.floor((diff / (1000 * 60)) % 60);
  const segundos = Math.floor((diff / 1000) % 60);

  document.getElementById("timer").innerText =
    `${dias} dias, ${horas}h ${minutos}min ${segundos}s`;
}

setInterval(atualizarTempo, 1000);
